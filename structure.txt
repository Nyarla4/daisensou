cost:text //cost+=player.costPerSec
btn:component
btn_dummy.onclick(()=>{
    if(cost<dummy.cost) return;
    cost-=dummy.cost;
    instantiate(dummy,playerBase);
})
dummy {
    moveLeftFromStartPos(move left dummy.moveSpeed per sec)
    targets=[];
    oncollider(c){
        if(c.side!=dummy.side){
            targets.push(c);
        }
    }
    attackTargets(damage targets dummy.attackDamage per dummy.attackTerm)
}